diff --git a/app/build.gradle.kts b/app/build.gradle.kts
index a32b666..e3f9ab1 100644
--- a/app/build.gradle.kts
+++ b/app/build.gradle.kts
@@ -99,3 +99,8 @@ tasks.register<JavaExec>("runRemotePairEvaluation") {
     val raw = project.findProperty("args")?.toString()
     args = if (raw != null) listOf(raw) else listOf("5001,5002,10,50")
 }
+
+tasks.register<JavaExec>("runGUI") {
+    mainClass.set("games.planetwars.view.RunVisualGameWithRemoteAgentKt")
+    classpath = sourceSets["main"].runtimeClasspath
+}
diff --git a/app/src/main/kotlin/games/planetwars/view/GameView.kt b/app/src/main/kotlin/games/planetwars/view/GameView.kt
index d62489d..6973ffc 100644
--- a/app/src/main/kotlin/games/planetwars/view/GameView.kt
+++ b/app/src/main/kotlin/games/planetwars/view/GameView.kt
@@ -14,8 +14,8 @@ class GameView(
     var paused: Boolean = false,
     var showInfoFor: Set<Player> = setOf(
         Player.Player1,
-//        Player.Player2,
-//        Player.Neutral,
+        Player.Player2,
+        Player.Neutral,
     ),
 ) : XApp {
 
diff --git a/app/src/main/kotlin/games/planetwars/view/RunVisualGameWithRemoteAgent.kt b/app/src/main/kotlin/games/planetwars/view/RunVisualGameWithRemoteAgent.kt
index a1868d7..2b6cbe7 100644
--- a/app/src/main/kotlin/games/planetwars/view/RunVisualGameWithRemoteAgent.kt
+++ b/app/src/main/kotlin/games/planetwars/view/RunVisualGameWithRemoteAgent.kt
@@ -1,19 +1,21 @@
 package games.planetwars.view
 
 import games.planetwars.agents.RemoteAgent
-import games.planetwars.agents.random.CarefulRandomAgent
+import competition_entry.GreedyHeuristicAgent
 import games.planetwars.core.GameParams
 import games.planetwars.runners.GameRunner
 import games.planetwars.core.GameStateFactory
 import xkg.jvm.AppLauncher
 
 fun main() {
-    val gameParams = GameParams(numPlanets = 20, maxTicks = 1000)
+    val gameParams = GameParams(numPlanets = 12, maxTicks = 1000)
     val gameState = GameStateFactory(gameParams).createGame()
-    val agent1 = CarefulRandomAgent()
-    // Use a remote agent that connects to a game agent server running on a specified port
-    // Be sure to start the server first
-    val agent2 = RemoteAgent("<specified by remote server>", port = 8090)
+    val agent1 = RemoteAgent("Sharp Agent", port = 8765)
+    val agent2 = GreedyHeuristicAgent()
+    println("Planet Wars")
+    println("Player 1 (red)\t: ${agent1.getAgentType()}")
+    println("Player 2 (blue)\t: ${agent2.getAgentType()}")
+    println("Neutral (gray)\t: neutral planets")
     val gameRunner = GameRunner(agent1, agent2, gameParams)
 
     val title = "${agent1.getAgentType()} : Planet Wars : ${agent2.getAgentType()}"
diff --git a/app/src/main/python/client_server/game_agent_server.py b/app/src/main/python/client_server/game_agent_server.py
index 304b878..a77d95c 100644
--- a/app/src/main/python/client_server/game_agent_server.py
+++ b/app/src/main/python/client_server/game_agent_server.py
@@ -9,12 +9,19 @@ from agents.greedy_heuristic_agent import GreedyHeuristicAgent
 from client_server.util import RemoteInvocationRequest, RemoteInvocationResponse, deserialize_args, serialize_result
 from core.game_state import Player, camel_to_snake
 
+import sys
+from pathlib import Path
+project_root = Path(__file__).resolve().parents[6]
+if str(project_root) not in sys.path:
+    sys.path.insert(0, str(project_root))
+
+from sharp_agent import SharpAgent
 
 class GameServerAgent:
     def __init__(self, host: str = "localhost", port: int = 8765):
         self.host = host
         self.port = port
-        self.agent_map: Dict[str, GreedyHeuristicAgent] = {}
+        self.agent_map = {}
 
     async def handler(self, websocket):
         async for message in websocket:
@@ -24,7 +31,7 @@ class GameServerAgent:
 
                 if request.requestType == "init":
                     agent_id = str(uuid.uuid4())
-                    agent = GreedyHeuristicAgent()
+                    agent = SharpAgent()
                     self.agent_map[agent_id] = agent
                     result = {"objectId": agent_id}
 
